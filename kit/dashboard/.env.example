# Copy to Render → the dashboard service → Environment. All are secrets.

# Better Stack Uptime API token (Settings → API tokens)
BETTERSTACK_API_TOKEN=

# PostHog personal API key, phx_... (Settings → Personal API keys, scope: query:read)
POSTHOG_PERSONAL_API_KEY=

# Already known — this account:
POSTHOG_PROJECT_ID=217283
POSTHOG_HOST=https://eu.posthog.com
